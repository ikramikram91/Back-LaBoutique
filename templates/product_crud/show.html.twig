{% extends 'admin.html.twig' %}



{% block body %}<div class="container">
    <h1 style="color:black; display:flex; text-align:center" >Détaille du produit </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ product.content }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.price }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ product.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('product_crud_index') }}">back to list</a>

    <a class="btn btn-dark" href="{{ path('product_crud_edit', {'id': product.id}) }}">edit</a>
    {{ include('product_crud/_delete_form.html.twig') }}
    </div>
{% endblock %}
